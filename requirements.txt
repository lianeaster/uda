# Версії зафіксовані під те, на чому проєкт розробляється й тестується.
# Оновлювати свідомо: спершу локально, потім на сервері.
Django==5.2.17
Pillow==12.3.0

# Прод: PostgreSQL і WSGI-сервер. Локально на SQLite вони не заважають.
psycopg[binary]==3.3.5
gunicorn==26.2.0
